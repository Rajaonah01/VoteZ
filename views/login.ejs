<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Connexion – VoteZ</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="shortcut icon" href="/images/voter.png" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #6f42c1, #0d6efd);
      min-height: 100vh;
      display: flex;
      align-items: center;
      color: #fff;
    }

    .login-card {
      border-radius: 20px;
      overflow: hidden;
    }

    .login-left {
      background: rgba(255,255,255,0.95);
      color: #000;
      padding: 40px;
    }

    .login-right {
      background: url('/images/21.png') center/contain no-repeat;
      background-color: rgba(218, 213, 213, 0.2);
    }

    .form-control {
      border-radius: 12px;
    }

    .btn-login {
      border-radius: 50px;
      font-size: 1.1rem;
      padding: 12px;
    }

    .brand {
      font-weight: 800;
      font-size: 1.8rem;
      color: #6f42c1;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .login-right {
        display: none;
      }

      .login-left {
        padding: 30px;
      }
    }
  </style>
</head>

<body>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <div class="card login-card shadow-lg">
        <div class="row g-0">

          <!-- Form -->
          <div class="col-md-6 login-left">
            <div class="text-center mb-4">
              <div class="brand">VoteZ pour les Gen Z</div>
              <p class="text-muted">Connecte-toi pour voter 🗳️</p>
            </div>

            <% if (error) { %>
              <div class="alert alert-danger"><%= error %></div>
            <% } %>

            <form method="POST" action="/login">
              <div class="mb-3">
                <label class="form-label">Email :</label>
                <input type="email" name="email" class="form-control" placeholder="ex: genz@email.com" required>
              </div>

              <div class="mb-3">
                <label class="form-label">Mot de passe :</label>
                <input type="password" name="motdepasse" class="form-control" placeholder="••••••••" required>
              </div>

              <button type="submit" class="btn btn-primary btn-login w-100 mt-2">
                Se connecter
              </button>
            </form>

            <p class="text-center mt-4">
              Pas encore de compte ?
              <a href="/register" class="fw-bold text-decoration-none">
                Créer un compte
              </a>
            </p>
          </div>

          <!-- Illustration -->
          <div class="col-md-6 login-right d-flex align-items-center justify-content-center">
          </div>

        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
