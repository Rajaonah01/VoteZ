<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Votez - VoteZ</title>
  <link rel="shortcut icon" href="/images/21.png" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f8f9fa, #e0f7fa);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: 700;
      color: #333;
    }

    .alert {
      max-width: 600px;
      margin: 0 auto 20px auto;
      text-align: center;
    }

    .candidats-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      max-width: 900px;
      margin: 0 auto 30px auto;
    }

    .candidat-card {
      background-color: #fff;
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      transition: transform 0.2s, box-shadow 0.2s;
      cursor: pointer;
    }

    .candidat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 15px rgba(0,0,0,0.2);
    }

    .candidat-card input[type="radio"] {
      display: none;
    }

    .candidat-card label {
      display: block;
      font-size: 1.2rem;
      font-weight: 600;
      cursor: pointer;
      padding: 10px;
      border-radius: 10px;
      transition: background-color 0.2s, color 0.2s;
    }

    /* Styles quand sélectionné */
    .candidat-card input[type="radio"]:checked + label {
      background-color: #00bcd4;
      color: #fff;
    }

    /* Bouton voter */
    .btn-voter {
      display: block;
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
      padding: 10px;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 10px;
      transition: background-color 0.2s;
    }

    .btn-voter:hover {
      background-color: #007c91;
    }
  </style>
</head>
<body>

  <h2>Votez pour votre candidat préféré</h2>

  <% if (message) { %>
    <div class="alert alert-warning"><%= message %></div>
  <% } else { %>
    <form method="POST" action="/vote">
      <div class="candidats-container">
        <% candidats.forEach(c => { %>
          <div class="candidat-card">
            <input type="radio" name="candidat_id" value="<%= c.id %>" id="candidat<%= c.id %>">
            <label for="candidat<%= c.id %>"><%= c.nom %></label>
          </div>
        <% }) %>
      </div>
      <button type="submit" class="btn btn-success btn-voter">Voter</button>
    </form>
  <% } %>

</body>
</html>
